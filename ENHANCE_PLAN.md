# Enhancement Plan — 2026-05-23
Generated by /plan-enhance-app
Source: EVALUATE_REPORT.md dated 2026-05-23 17:55 UTC

## Overview

40 findings remain after fixing all 5 Tier 1 Critical items (price cache, PyJWT migration, auth fail-loud, SSE reconnect, portfolio_value fix). The plan spans 8 sessions across security hardening, performance optimization, feature additions, and architecture cleanup. The most critical remaining work is MCP environment isolation (Critical severity, Tier 2) and the page-reload reconnect feature.

## Already Resolved (This Session)

| # | Finding | Commit |
|---|---------|--------|
| 1 | Price TTL cache | 177e0ec |
| 2 | AUTH_SECRET_KEY fail-loud | 177e0ec |
| 3 | python-jose → PyJWT | 177e0ec |
| 4 | SSE reconnection logic | 177e0ec |
| 5 | portfolio_value missing prices | 177e0ec |
| 16 | test_start_twice fixed | 177e0ec |
| 20 | AUTH_SECRET_KEY copy-by-value in auth_routes | 177e0ec |
| 23 | get_state_impl vs portfolio_value inconsistency | 177e0ec |
| 34 | jose/bcrypt silent import fallback | 177e0ec |

## Plan Summary

| Session | Name | Tier | Findings | Effort | Domain |
|---------|------|------|----------|--------|--------|
| 1 | Security Hardening | 2 | 5 | Medium | Security |
| 2 | Security Deep Fixes | 2-3 | 5 | Medium | Security |
| 3 | Performance Quick Wins | 2-3 | 5 | Low | Performance |
| 4 | Game Reconnect & Error UX | 2-3 | 5 | Medium | Feature |
| 5 | Game History & Leaderboard | 2-3 | 3 | Medium | Feature |
| 6 | Architecture & Test Cleanup | 3 | 5 | Medium | Architecture |
| 7 | Frontend Polish | 3 | 5 | Low | Feature/Perf |
| 8 | Infrastructure & Deferred | 3 | 7 | High | Mixed |

## Dependency Map

```
Session 1 (Security) ──→ Session 2 (Security Deep)
                    └──→ Session 4 (Reconnect) ──→ Session 7 (Frontend Polish)
Session 3 (Performance) ──→ Session 4 (Reconnect)
Session 5 (History)         — independent
Session 6 (Architecture)    — independent
Session 8 (Infrastructure)  — independent (do last)
```

## Session Detail

---
### Session 1 — Security Hardening
Tier      : 2
Domain    : Security
Effort    : Medium
Depends on: none
Blocks    : Session 2, Session 4

#### Findings addressed
| # | Finding | Score | Severity |
|---|---------|-------|----------|
| 6 | Full process environment leaked to MCP subprocesses | 5 | Critical |
| 7 | Docker container runs as root | 5 | High |
| 14 | Admin delete accepts path-style usernames | 4 | High |
| 15 | No failed login logging or auth audit trail | 4 | Medium |
| 33 | Username normalization inconsistency | 3 | Medium |

#### Goal
MCP subprocesses receive only the env vars they need, Docker runs as a non-root user, admin endpoints validate input, login attempts are logged, and usernames are normalized consistently.

#### Files likely to change
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/traders/mcp_servers.py` — whitelist env vars per subprocess
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/routers/admin_routes.py` — username validation, path param fix
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/routers/auth_routes.py` — login logging, username normalization on create
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/auth.py` — normalize in authenticate_user and create flows
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/Dockerfile` — add USER directive

#### Acceptance criteria
- `mcp_servers.py` passes only `MASSIVE_API_KEY` + `PATH` to Massive MCP, only `PATH` + `NODE_PATH` + `MEMORY_FILE_PATH` to Memory MCP
- `Dockerfile` includes `RUN adduser --disabled-password appuser` and `USER appuser`
- Admin delete uses `{username}` not `{username:path}`, with regex validation `^[a-zA-Z0-9_.@+-]+$`
- Failed login attempts logged with `logger.warning` including username and IP
- Successful logins logged with `logger.info`
- `create_user` normalizes username with `.strip().lower()` before storing
- All existing tests pass (`DEV_MODE=true uv run pytest --ignore=backend/test/test_prices.py`)

#### Start here prompt
```
Read ENHANCE_PLAN.md and EVALUATE_REPORT.md in /home/kent_benson/TRADEVIEW-DONNER/tradewars.

This is Session 1 — Security Hardening. Fix these 5 findings:

1. Finding #6: Whitelist env vars for MCP subprocesses in backend/traders/mcp_servers.py.
   Massive MCP needs: MASSIVE_API_KEY, PATH. Memory MCP needs: PATH, NODE_PATH, MEMORY_FILE_PATH.
   Do NOT pass dict(os.environ) or {**os.environ}.

2. Finding #7: Add non-root user to Dockerfile.
   Add `RUN adduser --disabled-password appuser` and `USER appuser` before CMD.

3. Finding #14: Fix admin delete route in backend/routers/admin_routes.py.
   Change {username:path} to {username}. Add username Field validation:
   pattern=r"^[a-zA-Z0-9_.@+-]+$" on CreateUserRequest.username.

4. Finding #15: Add login logging in backend/routers/auth_routes.py.
   Log failed logins with logger.warning (username + IP). Log successful logins with logger.info.

5. Finding #33: Normalize usernames on creation in backend/routers/admin_routes.py.
   Apply .strip().lower() before storing, matching authenticate_user behavior.

Run: DEV_MODE=true uv run pytest --ignore=backend/test/test_prices.py
All tests must pass. Rebuild frontend if needed. Restart server.
Run /wrapup when complete.
```

---
### Session 2 — Security Deep Fixes
Tier      : 2-3
Domain    : Security
Effort    : Medium
Depends on: Session 1
Blocks    : none

#### Findings addressed
| # | Finding | Score | Severity |
|---|---------|-------|----------|
| 12 | JWT token exposed in URL query parameter for SSE | 4 | High |
| 13 | No account lockout after failed login attempts | 4 | High |
| 30 | No security headers (HSTS, CSP, X-Frame-Options) | 3 | Medium |
| 32 | No financial transaction audit trail | 3 | Medium |
| 35 | User credentials in JSON with no file locking | 3 | High |

#### Goal
SSE uses short-lived tickets instead of full JWT in URL, failed logins trigger progressive lockout, security headers are set, games record which user started them, and user file writes are locked.

#### Files likely to change
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/auth.py` — SSE ticket exchange, lockout counter, file locking
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/routers/auth_routes.py` — ticket endpoint, lockout logic
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/api/app.py` — security headers middleware, pass username to arena start
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/environment/accounts.py` — initiated_by column in games table
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/frontend/src/api.ts` — fetch SSE ticket before opening stream

#### Acceptance criteria
- SSE endpoint accepts a short-lived ticket (30s TTL) instead of the full JWT
- Frontend exchanges JWT for ticket via `POST /api/auth/sse-ticket` before opening EventSource
- Failed login lockout: 5 failures in 10 minutes → 15-minute lockout per username
- Security headers middleware adds HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy
- `games` table has `initiated_by TEXT` column recording authenticated username
- `_save_users` uses `fcntl.flock` for write locking
- All tests pass

#### Start here prompt
```
Read ENHANCE_PLAN.md and EVALUATE_REPORT.md in /home/kent_benson/TRADEVIEW-DONNER/tradewars.

This is Session 2 — Security Deep Fixes. Fix these 5 findings:

1. Finding #12: Replace JWT-in-URL for SSE with short-lived tickets.
   Add POST /api/auth/sse-ticket that returns a 30-second single-use ticket.
   Backend middleware accepts ?ticket= for /arena/stream, validates and consumes it.
   Frontend calls sse-ticket endpoint before opening EventSource.

2. Finding #13: Add account lockout in backend/auth.py.
   Track failed attempts per username (in-memory dict with timestamps).
   After 5 failures in 10 minutes, return 429 for 15 minutes.

3. Finding #30: Add security headers middleware in backend/api/app.py.
   Set: Strict-Transport-Security, X-Content-Type-Options: nosniff,
   X-Frame-Options: DENY, Referrer-Policy: strict-origin-when-cross-origin.

4. Finding #32: Add initiated_by to games table in backend/environment/accounts.py.
   Pass authenticated username from the start endpoint through arena to record_game.

5. Finding #35: Add file locking to _save_users in backend/auth.py.
   Use fcntl.flock(f, fcntl.LOCK_EX) around writes to users.json.

Run tests, rebuild frontend, restart server. Run /wrapup when complete.
```

---
### Session 3 — Performance Quick Wins
Tier      : 2-3
Domain    : Performance
Effort    : Low
Depends on: none
Blocks    : Session 4

#### Findings addressed
| # | Finding | Score | Severity |
|---|---------|-------|----------|
| 8 | _load_users() reads JSON on every auth check | 5 | High |
| 9 | Log view re-renders entire DOM on every SSE event | 5 | High |
| 24 | SSE stream never terminates after game end | 3 | Medium |
| 27 | No trades table index on trader_id | 3 | Medium |
| 28 | SQLite not using WAL mode | 3 | Medium |

#### Goal
Auth checks use cached user data, log view appends incrementally, SSE streams terminate cleanly, and SQLite is optimized with WAL mode and proper indexes.

#### Files likely to change
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/auth.py` — cache _load_users with mtime check
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/frontend/src/log.ts` — append-only rendering
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/arena/arena.py` — sentinel event on end(), stream() termination
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/environment/accounts.py` — WAL pragma, trades index

#### Acceptance criteria
- `_load_users()` caches parsed dict, re-reads only when file mtime changes
- `LogView.render()` appends new entries only, does not wipe innerHTML
- `Arena.end()` puts a sentinel event on the queue; `stream()` breaks on sentinel
- `CREATE INDEX IF NOT EXISTS idx_trades_trader ON trades(trader_id)` in SCHEMA
- `PRAGMA journal_mode=WAL` executed after connect in Accounts.__init__
- All tests pass, frontend rebuilt

#### Start here prompt
```
Read ENHANCE_PLAN.md and EVALUATE_REPORT.md in /home/kent_benson/TRADEVIEW-DONNER/tradewars.

This is Session 3 — Performance Quick Wins. Fix these 5 findings:

1. Finding #8: Cache _load_users() in backend/auth.py.
   Store parsed dict + file mtime. On next call, check os.path.getmtime —
   only re-read if file changed. Thread-safe (simple — single process).

2. Finding #9: Make LogView append-only in frontend/src/log.ts.
   Instead of innerHTML="" + rebuild all rows, append only new entries.
   Track last rendered index. Only full-rebuild when log wraps.

3. Finding #24: Terminate SSE stream on game end in backend/arena/arena.py.
   Define a sentinel (e.g. None). In end(), put sentinel on self.events queue.
   In stream(), break the while-True loop when sentinel is received.

4. Finding #27: Add trades index in backend/environment/accounts.py SCHEMA.
   Add: CREATE INDEX IF NOT EXISTS idx_trades_trader ON trades(trader_id);

5. Finding #28: Enable WAL mode in Accounts.__init__.
   After connect: self.conn.execute("PRAGMA journal_mode=WAL")

Run: DEV_MODE=true uv run pytest --ignore=backend/test/test_prices.py
Rebuild frontend. Restart server. Run /wrapup when complete.
```

---
### Session 4 — Game Reconnect & Error UX
Tier      : 2-3
Domain    : Feature
Effort    : Medium
Depends on: Session 3 (SSE termination)
Blocks    : Session 7

#### Findings addressed
| # | Finding | Score | Severity |
|---|---------|-------|----------|
| 10 | No page reload/reconnect for running games | 5 | Critical |
| 11 | No error toast/user notification in frontend | 4 | Medium |
| 36 | Tick endpoint has no rate limiting | 3 | Medium |
| 37 | No health check endpoint | 3 | Medium |
| 38 | No confirmation dialog before starting new game | 3 | Medium |

#### Goal
Refreshing the browser mid-game rehydrates the UI, errors show visible toasts, tick is rate-limited, a health endpoint exists, and starting a new game while one runs requires confirmation.

#### Files likely to change
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/api/app.py` — /arena/status endpoint, health endpoint, tick rate limit
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/frontend/src/main.ts` — boot() checks status, rehydrate panels, confirm dialog, toast system
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/frontend/src/styles.css` — toast styles, confirm dialog styles

#### Acceptance criteria
- `GET /arena/status` returns `{running: bool, snapshot?: ArenaSnapshot}`
- `boot()` calls `/arena/status` on load; if running, creates panels and starts tick/stream
- Error toast appears in top-right corner for 5s on any API/SSE failure
- `POST /arena/tick` rate-limited to 2/second per IP
- `GET /health` returns `{status: "ok", uptime_seconds: N}`
- Start button shows confirm dialog if a game is already running
- All tests pass, frontend rebuilt

#### Start here prompt
```
Read ENHANCE_PLAN.md and EVALUATE_REPORT.md in /home/kent_benson/TRADEVIEW-DONNER/tradewars.

This is Session 4 — Game Reconnect & Error UX. Fix these 5 findings:

1. Finding #10: Add GET /arena/status endpoint in backend/api/app.py.
   Return {running: bool, snapshot?: ArenaSnapshot}. In frontend main.ts boot(),
   call /arena/status after auth. If running, create panels from snapshot traders,
   start tick loop and SSE stream to rehydrate the game.

2. Finding #11: Add toast notification system in frontend.
   Simple div positioned top-right, slides in on error, auto-dismisses after 5s.
   Wire into startArena catch, tickArena catch, and SSE onerror.

3. Finding #36: Rate-limit POST /arena/tick to 2/second in backend/api/app.py.
   Use @limiter.limit("2/second").

4. Finding #37: Add GET /health endpoint (no auth required).
   Return {status: "ok", uptime_seconds: N}. Exclude from auth middleware.

5. Finding #38: Add confirmation dialog before starting a new game.
   In main.ts, if game is running when Start is clicked, show a confirm()
   before calling startArena().

Run tests, rebuild frontend, restart server. Verify reconnect works by
refreshing browser mid-game. Run /wrapup when complete.
```

---
### Session 5 — Game History & Leaderboard
Tier      : 2-3
Domain    : Feature
Effort    : Medium
Depends on: none
Blocks    : none

#### Findings addressed
| # | Finding | Score | Severity |
|---|---------|-------|----------|
| 18 | No past games history view in UI | 4 | High |
| 19 | No cost tracking / token usage dashboard | 4 | High |
| 39 | No leaderboard/score summary at game end | 3 | Medium |

#### Goal
Users can view past game results, see per-trader token usage during games, and get a summary overlay when a game ends.

#### Files likely to change
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/api/app.py` — /arena/history endpoint
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/traders/trader.py` — capture result.usage token counts
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/arena/arena.py` — pass token usage to TraderSnapshot/events
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/frontend/src/main.ts` — history view, end-game leaderboard overlay

#### Acceptance criteria
- `GET /arena/history` returns list of past games with P&L per trader
- Token usage (input/output/cache) captured per decision cycle and emitted in events
- End-of-game overlay shows ranked traders with P&L, trade count, and total tokens
- History accessible from sidebar (collapsible section or modal)
- All tests pass, frontend rebuilt

#### Start here prompt
```
Read ENHANCE_PLAN.md and EVALUATE_REPORT.md in /home/kent_benson/TRADEVIEW-DONNER/tradewars.

This is Session 5 — Game History & Leaderboard. Fix these 3 findings:

1. Finding #18: Add GET /arena/history endpoint in backend/api/app.py.
   Call accounts.list_games(). Return the game history as JSON.
   Add a history view in the frontend — either a sidebar section or modal
   showing past games with date, duration, and P&L per trader.

2. Finding #19: Capture token usage from the SDK in backend/traders/trader.py.
   After Runner.run_streamed completes, extract result.usage (input_tokens,
   output_tokens, cache_creation_input_tokens, cache_read_input_tokens).
   Emit as a "usage" event or include in cycle_end events.

3. Finding #39: Add end-of-game leaderboard overlay.
   When game ends (detected in teardown or tick where running=false),
   show a modal overlay with traders ranked by P&L, showing final value,
   P&L, trade count, and winner highlight.

Run tests, rebuild frontend, restart server. Run /wrapup when complete.
```

---
### Session 6 — Architecture & Test Cleanup
Tier      : 3
Domain    : Architecture
Effort    : Medium
Depends on: none
Blocks    : none

#### Findings addressed
| # | Finding | Score | Severity |
|---|---------|-------|----------|
| 21 | Private _load_users/_save_users imported cross-module | 3 | Medium |
| 22 | os.environ["KEY"] crashes with unhelpful KeyError | 3 | Medium |
| 25 | No auth/admin route tests | 3 | Medium |
| 41 | test_prices fails with NOT_AUTHORIZED (needs better skip) | 2 | Medium |
| 42 | No conftest.py — test fixtures duplicated | 2 | Low |

#### Goal
Auth module has a clean public API, missing env vars give helpful errors, auth routes are tested, price tests skip gracefully, and shared test fixtures live in conftest.py.

#### Files likely to change
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/auth.py` — make load/save public or add facade functions
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/traders/models.py` — wrap os.environ[] with helpful error
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/test/conftest.py` — new file with shared fixtures
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/test/test_auth_routes.py` — new test file
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/test/test_prices.py` — better skip logic

#### Acceptance criteria
- `_load_users` and `_save_users` either renamed to public or wrapped in public functions
- Router modules import only public API from auth.py
- `models.py` wraps `os.environ["KEY"]` with `RuntimeError(f"{key} not set")`
- `conftest.py` contains FakePrices, neutralize_trader_loop, disable_auth, accounts fixtures
- Duplicate fixtures removed from test_api.py, test_arena.py, test_tools.py
- `test_auth_routes.py` covers: login success, login failure, /me, change-password, rate limiting
- `test_prices.py` skips gracefully on auth errors (catch BadResponse, not just missing key)
- All tests pass

#### Start here prompt
```
Read ENHANCE_PLAN.md and EVALUATE_REPORT.md in /home/kent_benson/TRADEVIEW-DONNER/tradewars.

This is Session 6 — Architecture & Test Cleanup. Fix these 5 findings:

1. Finding #21: Make _load_users/_save_users public in backend/auth.py.
   Rename to load_users/save_users. Update imports in auth_routes.py and admin_routes.py.

2. Finding #22: Wrap os.environ["KEY"] in backend/traders/models.py.
   Add a helper: def _require_env(key): v = os.environ.get(key); if not v: raise RuntimeError(f"{key} is not set"); return v
   Use it at lines 69, 74, 79, 88.

3. Finding #25: Create backend/test/test_auth_routes.py.
   Test: login success, login failure (401), /me with valid token, /me without token (401),
   change-password, rate limiting on login.

4. Finding #41: Fix test_prices.py skip logic.
   Catch massive.exceptions.BadResponse in addition to missing key.
   Use pytest.importorskip or a broader skip condition.

5. Finding #42: Create backend/test/conftest.py.
   Move FakePrices, neutralize_trader_loop, disable_auth, accounts fixtures there.
   Remove duplicates from test_api.py, test_arena.py, test_tools.py.

Run: DEV_MODE=true uv run pytest --ignore=backend/test/test_prices.py
All tests must pass. Run /wrapup when complete.
```

---
### Session 7 — Frontend Polish
Tier      : 3
Domain    : Feature / Performance
Effort    : Low
Depends on: Session 3 (log changes), Session 4 (error handling)
Blocks    : none

#### Findings addressed
| # | Finding | Score | Severity |
|---|---------|-------|----------|
| 17 | OPENROUTER_API_KEY empty — Kimi K2.6 broken | 4 | High |
| 26 | panel.update() redraws chart+heatmap on every SSE event | 3 | Medium |
| 45 | No token usage metrics exposed to frontend | 2 | Medium |
| 46 | Heatmap doesn't show P&L percentage | 2 | Low |
| 53 | No favicon | 1 | Low |

#### Goal
Kimi K2.6 works, panel updates are efficient, heatmap shows P&L%, and the app has a favicon.

#### Files likely to change
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/.env` — set OPENROUTER_API_KEY
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/frontend/src/panel.ts` — split update into updateLog/updateData
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/frontend/src/heatmap.ts` — show unrealized_pnl percentage
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/frontend/index.html` — favicon link

#### Acceptance criteria
- OPENROUTER_API_KEY is set in .env (user provides the key)
- `panel.update()` split: SSE events only update log; tick snapshots update chart+heatmap+stats
- Heatmap tiles show P&L % below market value
- Favicon present in index.html
- Frontend rebuilt, server restarted

#### Start here prompt
```
Read ENHANCE_PLAN.md and EVALUATE_REPORT.md in /home/kent_benson/TRADEVIEW-DONNER/tradewars.

This is Session 7 — Frontend Polish. Fix these 5 findings:

1. Finding #17: Ask the user for their OpenRouter API key.
   Add OPENROUTER_API_KEY to .env. Verify Kimi K2.6 appears in the UI dropdown.

2. Finding #26: Split panel.update() in frontend/src/panel.ts.
   Add updateLog() that only updates the log view.
   update() only called from tick snapshots (not every SSE event).
   In main.ts onTraderEvent, call panel.updateLog() instead of panel.update().

3. Finding #45: Display token usage if available.
   If Session 5 added usage events, show cumulative tokens in cycle stats area.

4. Finding #46: Show P&L % on heatmap tiles in frontend/src/heatmap.ts.
   Use HoldingDetail.unrealized_pnl and avg_cost to compute and display percentage.

5. Finding #53: Add a favicon to frontend/index.html.
   Generate a simple trading-themed SVG favicon inline.

Rebuild frontend, restart server. Run /wrapup when complete.
```

---
### Session 8 — Infrastructure & Deferred
Tier      : 3
Domain    : Mixed
Effort    : High
Depends on: none
Blocks    : none

#### Findings addressed
| # | Finding | Score | Severity |
|---|---------|-------|----------|
| 29 | MCP subprocess no health monitoring or recovery | 3 | High |
| 31 | 7-day JWT expiry with no refresh mechanism | 3 | Medium |
| 40 | No structured logging or log rotation | 2 | Medium |
| 43 | Synchronous HTTP wrapped in asyncio.to_thread | 2 | Medium |
| 44 | No mobile/responsive layout | 2 | Medium |
| 47 | Supply chain: curl pipe bash, git tags, npx -y | 2 | Low |
| 48-51 | Minor DRY violations (FakePrices, _now, REPO_ROOT, ipykernel) | 1 | Low |

#### Goal
MCP subprocesses are monitored and can recover, JWT tokens have reasonable expiry with refresh, logging is structured, and remaining polish items are addressed.

#### Files likely to change
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/traders/trader.py` — MCP health check and restart
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/auth.py` — refresh token endpoint
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/api/app.py` — structured logging setup
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/backend/environment/prices.py` — async HTTP client
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/frontend/src/styles.css` — responsive media queries
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/Dockerfile` — pin npm versions, use node base image
- `/home/kent_benson/TRADEVIEW-DONNER/tradewars/pyproject.toml` — move ipykernel/sympy to dev deps

#### Acceptance criteria
- MCP subprocess crash triggers automatic restart (max 3 retries per game)
- JWT access token reduced to 1 hour, refresh token added (7 days)
- Logging uses JSON format with request correlation IDs
- ipykernel and sympy moved to `[dependency-groups] dev`
- _now(), REPO_ROOT consolidated into shared utility module
- FakePrices consolidated (if not already done in Session 6)

#### Start here prompt
```
Read ENHANCE_PLAN.md and EVALUATE_REPORT.md in /home/kent_benson/TRADEVIEW-DONNER/tradewars.

This is Session 8 — Infrastructure & Deferred. Address these remaining findings:

1. Finding #29: Add MCP health monitoring in backend/traders/trader.py.
   Wrap MCP context managers with retry logic. If subprocess dies, restart it
   and re-create the Agent (max 3 retries per game). Log each restart.

2. Finding #31: Reduce JWT expiry and add refresh tokens.
   Access token: 1 hour. Refresh token: 7 days. Add POST /api/auth/refresh endpoint.
   Frontend auto-refreshes token before expiry.

3. Finding #40: Add structured JSON logging in backend/api/app.py.
   Use logging.config.dictConfig or structlog. Include timestamp, level, module, message.

4. Finding #43: Consider replacing asyncio.to_thread with httpx.AsyncClient
   in backend/environment/prices.py for true async HTTP.

5. Finding #44: Add basic responsive layout — collapse sidebar on narrow screens.

6. Finding #47: Pin npm package version for Memory MCP. Pin git commit SHA for mcp_massive.

7. Findings #48-51: Move ipykernel/sympy to dev deps. Consolidate _now() and REPO_ROOT.

Run tests, rebuild frontend, restart server. Run /wrapup when complete.
After this session, re-run /evaluate-app to verify improvement.
```

---

## Completion Criteria

- All 8 sessions completed and verified
- All Tier 1 findings resolved (done — commit 177e0ec)
- All Tier 2 findings resolved (Sessions 1-5)
- Tier 3 findings addressed where practical (Sessions 6-8)
- /evaluate-app re-run showing no Critical or High severity findings
- All acceptance criteria met across all sessions
- Test suite fully green (excluding external API auth issues)

## Plan Metadata
| Field            | Value                       |
|------------------|-----------------------------|
| Generated        | 2026-05-23 18:45 UTC        |
| Source report     | /home/kent_benson/TRADEVIEW-DONNER/tradewars/EVALUATE_REPORT.md |
| Total sessions   | 8                           |
| Total findings   | 40 remaining (9 already resolved) |
| Tier 1 sessions  | 0 (all 5 Tier 1 done)       |
| Tier 2 sessions  | 4 (Sessions 1-4)            |
| Tier 3 sessions  | 4 (Sessions 5-8)            |
| Skill version    | plan-enhance-app v1.0       |
